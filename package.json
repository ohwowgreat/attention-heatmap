{
  "name": "attention-heatmap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv HEATMAP_KV",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
